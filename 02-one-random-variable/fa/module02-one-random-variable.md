<!-- TRANSLATION START -->
# ماژول 2 — یک متغیر تصادفی و توابع آن

## اهداف یادگیری

پس از تکمیل این ماژول، دانشجویان قادر خواهند بود:

1. متغیرهای تصادفی را تعریف کنند و نقش آن‌ها را در تحلیل مهندسی توضیح دهند
2. میان متغیرهای تصادفی گسسته و پیوسته تمایز قائل شوند
3. PMF، PDF و CDF را محاسبه و تفسیر کنند
4. تفاوت میان جرم احتمال، چگالی و احتمال تجمعی را به‌روشنی توضیح دهند
5. تبدیل‌ها را روی متغیرهای تصادفی اعمال کنند
6. از MATLAB برای تولید، مصورسازی و تحلیل متغیرهای تصادفی استفاده کنند

---

## 2.1 مقدمه: چرا متغیرهای تصادفی؟

### مسئله

در ماژول 1 با رویدادها کار کردیم — زیرمجموعه‌هایی از یک فضای نمونه. اما تحلیل مهندسی به **اعداد** نیاز دارد:

- چه مقدار ولتاژ نویز وجود دارد؟
- چند بسته رسید؟
- توان سیگنال دریافتی چقدر است؟

یک **متغیر تصادفی** خروجی‌های یک آزمایش تصادفی را به اعداد حقیقی نگاشت می‌کند و به این ترتیب امکان استفاده از تمام ابزارهای حساب دیفرانسیل و انتگرال و جبر را برای ما فراهم می‌کند.

### انگیزه مهندسی

| می‌خواهیم تحلیل کنیم... | یک متغیر تصادفی تعریف می‌کنیم... |
|----------------------|-------------------------------|
| نویز در یک مدار | $X$ = ولتاژ نویز (ولت) |
| خطاهای بیت در یک فریم | $N$ = تعداد خطاهای بیت |
| عمر قطعه | $T$ = زمان تا خرابی (ساعت) |
| دامنه سیگنال | $A$ = دامنه دریافتی $(V)$ |
| تأخیر بسته | $D$ = تأخیر سرتاسری $(ms)$ |

---

## 2.2 تعریف متغیر تصادفی

### تعریف صوری

یک **متغیر تصادفی** $X$ تابعی است که هر خروجی $\omega$ در فضای نمونه $S$ را به یک عدد حقیقی نگاشت می‌کند:

$$X: S \rightarrow \mathbb{R}$$
$$\omega \mapsto X(\omega)$$

### تفسیر

متغیر تصادفی به هر خروجی ممکن آزمایش یک مقدار عددی نسبت می‌دهد. تصادفی‌بودن از این ناشی می‌شود که کدام خروجی $\omega$ رخ می‌دهد — به‌محض تعیین $\omega$، مقدار $X(\omega)$ یک عدد معیّن است.

### مثال مهندسی

**آزمایش:** ارسال 5 بیت روی یک کانال نویزی.
**فضای نمونه:** $S$ = تمام دنباله‌های ممکن درست/خطا برای 5 بیت ($2^{5} = 32$ خروجی)
**متغیر تصادفی:** $X$ = تعداد خطاهای بیت

| خروجی $\omega$ | $X(\omega)$ |
|-----------|------|
| $(C,\,C,\,C,\,C,\,C)$ | 0 |
| $(E,\,C,\,C,\,C,\,C)$ | 1 |
| $(E,\,E,\,C,\,C,\,C)$ | 2 |
| ... | ... |
| $(E,\,E,\,E,\,E,\,E)$ | 5 |

حالا به‌جای کار با 32 خروجی، با یک عدد منفرد $X \in \{0,\,1,\,2,\,3,\,4,\,5\}$ کار می‌کنیم.

---

## 2.3 متغیرهای تصادفی گسسته

### تعریف

متغیر تصادفی $X$ **گسسته** است اگر مقادیر خود را از یک مجموعه **شمارا** (متناهی یا شمارای نامتناهی) بگیرد.

### ویژگی‌ها
- مقادیر را می‌توان فهرست کرد: $\{x_{1},\, x_{2},\, x_{3},\, \ldots \}$
- هر مقدار یک احتمال مشخص دارد (یک «جرم» از احتمال)
- مجموع تمام احتمال‌ها = 1

### مثال‌های مهندسی

| متغیر تصادفی | مقادیر ممکن | زمینه |
|----------------|-----------------|---------|
| تعداد خطاهای بیت | $\{0,\, 1,\, 2,\, \ldots,\, n\}$ | انتقال بسته |
| تعداد ارسال‌های مجدد | {0, 1, 2, ...} | پروتکل ARQ |
| سطح ولتاژ کوانتیزه‌شده | $\{0,\, 1,\, \ldots,\, 2^{N}-1\}$ | خروجی مبدل آنالوگ به دیجیتال N-بیتی |
| تعداد ورودها در هر ثانیه | {0, 1, 2, ...} | ترافیک شبکه |
| تعداد تراشه‌های معیوب | $\{0,\, 1,\, \ldots,\, n\}$ | آزمون ویفر |

---

## 2.4 متغیرهای تصادفی پیوسته

### تعریف

متغیر تصادفی $X$ **پیوسته** است اگر مقادیر خود را از یک مجموعه **ناشمارا** (بازه‌ای از اعداد حقیقی) بگیرد.

### ویژگی‌ها
- $X$ می‌تواند هر مقدار در یک محدوده را اختیار کند (مثلاً $[0,\, \infty)$ یا $(-\infty,\, \infty)$)
- احتمال هر مقدار دقیق منفرد صفر است: $P(X = x) = 0$
- احتمال‌ها برای بازه‌ها تعریف می‌شوند: $P(a \le X \le b)$

### مثال‌های مهندسی

| متغیر تصادفی | محدوده | زمینه |
|----------------|-------|---------|
| ولتاژ نویز حرارتی | $(-\infty,\, \infty)$ | نویز مدار |
| زمان تا خرابی | $[0,\, \infty)$ | قابلیت اطمینان |
| فاز سیگنال دریافتی | $[0,\, 2\pi)$ | مخابرات |
| مقدار مقاومت | $(R_{0} - \delta,\, R_{0} + \delta)$ | ساخت و تولید |
| بهره کانال | $[0,\, \infty)$ | محوشدگی بی‌سیم |

### چرا برای متغیرهای تصادفی پیوسته $P(X = x) = 0$ است

تصور کنید یک ولتاژ را اندازه‌گیری می‌کنید. احتمال این‌که $V$ دقیقاً 3.14159265... ولت باشد (با دقت نامتناهی) صفر است. تنها می‌توانیم درباره بازه‌ها بپرسیم: $P(3.14 < V < 3.15)$.

---

## 2.5 تابع جرم احتمال (PMF)

### تعریف

برای یک متغیر تصادفی گسسته $X$، **تابع جرم احتمال** عبارت است از:

$$p_X(x) = P(X = x)$$

### ویژگی‌ها

1. **نامنفی بودن:** $p_{X}(x) \ge 0$ برای تمام $x$
2. **نرمال‌سازی:** $\sum p_{X}(x_{i}) = 1$ (جمع روی تمام مقادیر ممکن)
3. **محاسبه احتمال:** $P(X \in A) = \sum_{x \in A} p_{X}(x)$

### مصورسازی

PMF به‌صورت **خطوط عمودی** (ضربه‌ها) در هر مقدار ممکن نمایش داده می‌شود، که ارتفاع آن برابر با احتمال است.

```{.figure #m02-pmf-stems}
p_X(x)
  |
0.4|     |
  |     |
0.3|  |  |
  |  |  |
0.2|  |  |  |
  |  |  |  |
0.1|  |  |  |  |
  |  |  |  |  |
  +--+--+--+--+--→ x
     0  1  2  3  4
```

### مثال مهندسی: خطاهای بیت در یک بایت

8 بیت ارسال می‌شود که هر بیت احتمال خطای مستقل $p = 0.1$ دارد.
$X$ = تعداد خطاهای بیت.

$p_{X}(k) = C(8,\,k) \cdot (0.1)^{k} \cdot (0.9)^{8-k}$, for $k = 0,\, 1,\, \ldots,\, 8$

| $k$ | $p_{X}(k)$ |
|---|--------|
| 0 | 0.4305 |
| 1 | 0.3826 |
| 2 | 0.1488 |
| 3 | 0.0331 |
| 4 | 0.0046 |
| 5+ | < 0.001 |

---

## 2.6 تابع چگالی احتمال (PDF)

### تعریف

برای یک متغیر تصادفی پیوسته $X$، **تابع چگالی احتمال** $f_{X}(x)$ به‌گونه‌ای تعریف می‌شود که:

$$P(a \leq X \leq b) = \int_a^b f_X(x) \, dx$$

### ویژگی‌ها

1. **نامنفی بودن:** $f_{X}(x) \ge 0$ برای تمام $x$
2. **نرمال‌سازی:** $\int_{-\infty}^{\infty} f_{X}(x)\,dx = 1$
3. **احتمال = مساحت زیر منحنی** میان دو نقطه

### نکته حیاتی: $f_{X}(x)$ یک احتمال نیست!

> ⚠️ $f_{X}(x)$ می‌تواند بزرگ‌تر از 1 باشد!

PDF **چگالی** احتمال را می‌دهد — احتمال در واحد طول. تنها **انتگرال** (مساحت) احتمال را می‌دهد.

**قیاس:** چگالی جرم (kg/m³) جرم نیست. برای به‌دست‌آوردن جرم باید چگالی را روی حجم انتگرال بگیرید. به‌همین‌ترتیب، چگالی احتمال هم احتمال نیست — برای به‌دست‌آوردن احتمال باید روی یک بازه انتگرال بگیرید.

### مصورسازی

```{.figure #m02-pdf-area}
f_X(x)
  |
  |      ╭────╮
  |     ╱ ░░░░ ╲        ← Shaded area = P(a ≤ X ≤ b)
  |    ╱ ░░░░░░ ╲
  |   ╱ ░░░░░░░░ ╲
  |──╱───░░░░░░░░──╲──→ x
  |      a      b
  Total area under curve = 1
```

### مثال مهندسی: ولتاژ نویز حرارتی

ولتاژ نویز حرارتی در یک مدار اغلب از توزیع گاوسی پیروی می‌کند:

$$f_X(x) = \frac{1}{\sigma\sqrt{2\pi}} e^{-x^2/(2\sigma^2)}$$

که در آن $\sigma$ ولتاژ مؤثر (RMS) نویز است.

$P(\text{noise exceeds 2}\sigma) = P(\lvert X\rvert > 2\sigma) = 1 - P(-2\sigma \le X \le 2\sigma) \approx 1 - 0.9545 = 0.0455$

---

## 2.7 تابع توزیع تجمعی (CDF)

### تعریف

برای هر متغیر تصادفی $X$ (گسسته یا پیوسته)، **تابع توزیع تجمعی** عبارت است از:

$$F_X(x) = P(X \leq x)$$

### ویژگی‌ها

1. **نامنزولی:** اگر $a < b$، آنگاه $F_{X}(a) \le F_{X}(b)$
2. **حدود:** $F_{X}(-\infty) = 0$، $F_{X}(+\infty) = 1$
3. **پیوستگی از راست:** $F_{X}(x)$ از سمت راست پیوسته است
4. **احتمال یک بازه:** $P(a < X \le b) = F_{X}(b) - F_{X}(a)$

### CDF برای متغیرهای تصادفی گسسته

CDF یک **تابع پله‌ای** با جهش‌هایی در هر مقدار ممکن است:

```{.figure #m02-cdf-discrete}
F_X(x)
  |
1.0|                          ──────────
  |                    ○─────┘
0.8|              ○────┘
  |         ○───┘
0.4|    ○───┘
  |────┘
0.0|
  +----+----+----+----+---→ x
       0    1    2    3
```

جهش در $x = k$ دارای ارتفاع $p_{X}(k)$ است.

### CDF برای متغیرهای تصادفی پیوسته

CDF یک منحنی **هموار و پیوسته** است:

$$F_X(x) = \int_{-\infty}^{x} f_X(t) \, dt$$

و برعکس:
$$f_X(x) = \frac{dF_X(x)}{dx}$$

```{.figure #m02-cdf-continuous}
F_X(x)
  |
1.0|                         ___________
  |                    ╱
  |                 ╱
0.5|             ╱
  |          ╱
  |       ╱
0.0|______╱
  +---+---+---+---+---+---→ x
```

### رابطه: $\mathrm{PDF}$ ↔ CDF

| جهت | فرمول | معنا |
|-----------|---------|---------|
| $\mathrm{PDF}$ → CDF | $F_{X}(x) = \int_{-\infty}^{x} f_{X}(t)dt$ | انتگرال‌گیری از چگالی برای به‌دست‌آوردن احتمال تجمعی |
| $\mathrm{CDF}$ → PDF | $f_{X}(x) = \frac{dF_{X}}{dx}$ | مشتق‌گیری از CDF برای به‌دست‌آوردن چگالی |



---

## 2.8 تمایز کلیدی: جرم در برابر چگالی در برابر تجمعی

این یکی از مهم‌ترین تمایزهای مفهومی در نظریه احتمال است.

### جدول مقایسه

| ویژگی | PMF $p_{X}(x)$ | PDF $f_{X}(x)$ | CDF $F_{X}(x)$ |
|----------|-----------|-----------|-----------|
| **برای** | فقط متغیرهای تصادفی گسسته | فقط متغیرهای تصادفی پیوسته | هر دو |
| **مستقیماً می‌دهد** | $P(X = x)$ | چگالی احتمال در $x$ | $P(X \le x)$ |
| **دامنه مقادیر** | [0, 1] | $[0,\, \infty)$ — می‌تواند از 1 فراتر رود! | [0, 1] |
| **جمع/انتگرال** | $\sum p_{X}(x) = 1$ | $\int f_{X}(x)dx = 1$ | از 0 شروع و به 1 ختم می‌شود |
| **به‌دست‌آوردن احتمال** | $P(X = x) = p_{X}(x)$ | $P(a \le X \le b) = \int f_{X}(x)dx$ | $P(a < X \le b) = F(b)-F(a)$ |

### قیاس بصری: انبار

احتمال را به‌عنوان «ماده‌ای» تصور کنید که روی مقادیر ممکن توزیع شده است:

**گسسته (PMF):** مانند انبار کردن ماده در جعبه‌های شماره‌گذاری‌شده. هر جعبه یک جرم مشخص دارد. PMF جرم هر جعبه را به شما می‌گوید.

```{.figure #m02-pmf-boxes}
    ┌───┐
    │0.4│ ┌───┐
    │   │ │0.3│ ┌───┐
    │   │ │   │ │0.2│ ┌───┐
    └───┘ └───┘ └───┘ └───┘
     x=0   x=1   x=2   x=3
     
    Total mass in all boxes = 1
```

**پیوسته (PDF):** مانند پهن‌کردن ماده در طول یک قفسه پیوسته. چگالی به شما می‌گوید ماده در هر نقطه چقدر ضخیم است. جرم را با محاسبه مساحت (ضخامت × عرض) به دست می‌آورید.

```{.figure #m02-pdf-area-12}
         ╭────────╮
        ╱          ╲
       ╱     ░░░░    ╲      ← Area of shaded region = P(1 ≤ X ≤ 2)
      ╱     ░░░░░░    ╲
    ─╱──────░░░░░░░░────╲──
          1      2

    Total area under curve = 1
```

**تجمعی (CDF):** مانند یک جمع در حال اجرا — تا این نقطه چه مقدار ماده انباشته‌اید؟

### چرا این موضوع در مهندسی اهمیت دارد

**مثال:** یک ولتاژ نویز $X$ دارای PDF $f_{X}(x) = 2$ برای $x \in [0,\, 0.5]$ است.

- $f_{X}(0.3) = 2$ — این یک احتمال نیست! به این معناست که چگالی برابر $2\,\mathrm{V}^{-1}$ است.
- $P(0.2 \le X \le 0.4) = \int_{0.2}^{0.4} 2\,dx = 2 \times 0.2 = 0.4$ — این یک احتمال است.
- $P(X = 0.3) = 0$ — برای متغیرهای تصادفی پیوسته، یک نقطه منفرد احتمال صفر دارد.

### خطاهای رایجی که باید از آن‌ها پرهیز کرد

| ❌ گزاره نادرست | ✅ گزاره درست |
|---|---|
| «احتمال در $x = 2$ برابر $f(2)$ است» | «چگالی احتمال در $x = 2$ برابر $f(2)$ است» |
| «$f(x)$ باید ≤ 1 باشد» | «$f(x)$ می‌تواند هر مقدار نامنفی باشد» |
| «برای $X$ پیوسته $P(X = 3) = f(3)$» | «برای $X$ پیوسته $P(X = 3) = 0$» |
| «PMF و PDF یک چیز هستند» | «PMF جرم می‌دهد؛ PDF چگالی می‌دهد» |

---

## 2.9 تبدیل‌های متغیرهای تصادفی

### انگیزه

در مهندسی، اغلب توزیع یک کمیت را می‌دانیم اما توزیع **تابعی** از آن کمیت را نیاز داریم:

- ولتاژ $V$ معلوم است → توان $P = \frac{V^{2}}{R}$ لازم است
- سیگنال خطی $X$ معلوم است → مقدار dB یعنی $Y = 10 \cdot \log_{10}(X)$ لازم است
- نویز $N$ معلوم است $\to \lvert N\rvert$ (نویز یکسوسازی‌شده) لازم است

### بیان مسئله

داده شده: $X$ با PDF معلوم $f_{X}(x)$ یا PMF معلوم $p_{X}(x)$
مطلوب: توزیع $Y = g(X)$

### حالت 1: تبدیل متغیر تصادفی گسسته

اگر $X$ گسسته با PMF $p_{X}(x)$ باشد و $Y = g(X)$:

$$p_Y(y) = \sum_{x: g(x)=y} p_X(x)$$

احتمال‌های تمام مقادیر $x$ را که به همان $y$ نگاشت می‌شوند جمع کنید.

**مثال:** $X \in \{-2,\, -1,\, 0,\, 1,\, 2\}$ با احتمال برابر 0.2 برای هر یک.
$Y = X^{2}$

| $Y = X^{2}$ | مقادیر $X$ که به $Y$ نگاشت می‌شوند | $p_{Y}(y)$ |
|---------|----------------------|---------|
| 0 | {0} | 0.2 |
| 1 | {-1, 1} | 0.4 |
| 4 | {-2, 2} | 0.4 |

### حالت 2: تبدیل یکنوا (پیوسته)

اگر $Y = g(X)$ که در آن $g$ اکیداً یکنوا و مشتق‌پذیر است:

$$f_Y(y) = f_X(g^{-1}(y)) \cdot \left|\frac{dg^{-1}(y)}{dy}\right|$$

یا به‌طور معادل:
$$f_Y(y) = \frac{f_X(x)}{|g'(x)|}\bigg|_{x=g^{-1}(y)}$$

**استخراج از طریق روش CDF:**
1. با CDF شروع کنید: $F_{Y}(y) = P(Y \le y) = P(g(X) \le y)$
2. وارون کنید: $= P(X \le g^{-1}(y))$ اگر $g$ صعودی باشد
3. مشتق بگیرید: $f_{Y}(y) = f_{X}(g^{-1}(y)) \cdot \frac{d}{dy}[g^{-1}(y)]$

### حالت 3: تبدیل غیریکنوا

اگر $g$ یکنوا نباشد، دامنه را به بازه‌هایی تقسیم کنید که در آن‌ها $g$ یکنوا است:

$$f_Y(y) = \sum_i \frac{f_X(x_i)}{|g'(x_i)|}$$

که در آن $x_{1},\, x_{2}$, ... همگی جواب‌های $g(x) = y$ هستند.

### مثال مهندسی: توان از ولتاژ

یک ولتاژ نویز $X$ دارای PDF $f_{X}(x)$ است (مثلاً گاوسی با میانگین صفر).
توان تلف‌شده در یک مقاومت 1Ω: $P = X^{2}$

از آنجا که $Y = X^{2}$ یکنوا نیست (هر دو $+x$ و $-x$ همان $y$ را می‌دهند):

برای $y > 0$: هر دو $x = +\sqrt{y}$ و $x = -\sqrt{y}$ جواب هستند.
$g(x) = x^{2}$، پس $g'(x) = 2x$

$$f_Y(y) = \frac{f_X(\sqrt{y})}{2\sqrt{y}} + \frac{f_X(-\sqrt{y})}{2\sqrt{y}}, \quad y > 0$$

اگر $X$ گاوسی با میانگین صفر باشد: $f_{X}(x) = f_{X}(-x)$، پس:
$$f_Y(y) = \frac{f_X(\sqrt{y})}{\sqrt{y}}, \quad y > 0$$

### مثال مهندسی: تقویت خطی

سیگنال $Y$ = aX $+ b$ (تقویت‌کننده با بهره $a$ و آفست b).
$X$ دارای PDF $f_{X}(x)$ است.

$g^{-1}(y) = \frac{y-b}{a}$، و $\lvert \frac{d}{dy}\left[\frac{y-b}{a}\right]\rvert = \frac{1}{\lvert a\rvert}$

$$f_Y(y) = \frac{1}{|a|} f_X\left(\frac{y-b}{a}\right)$$

---

## 2.10 مثال‌های MATLAB

### مثال 1: رسم یک PMF

```matlab
%% PMF of Number of Bit Errors in a Byte
n = 8;           % bits per byte
p = 0.1;         % bit error probability
k = 0:n;         % possible values

% Compute PMF (binomial distribution)
pmf = binopdf(k, n, p);

% Plot
figure;
stem(k, pmf, 'filled', 'LineWidth', 2);
xlabel('Number of Errors (k)');
ylabel('P(X = k)');
title('PMF: Bit Errors in 8-bit Byte (p = 0.1)');
grid on;

% Verify normalization
fprintf('Sum of PMF = %.6f (should be 1)\n', sum(pmf));
```

### مثال 2: رسم یک PDF و بررسی ویژگی‌ها

```matlab
%% PDF of Gaussian Noise Voltage
sigma = 0.5;     % RMS noise voltage
x = linspace(-3*sigma, 3*sigma, 1000);

% Compute PDF
pdf_x = normpdf(x, 0, sigma);

% Plot
figure;
plot(x, pdf_x, 'b-', 'LineWidth', 2);
xlabel('Voltage (V)');
ylabel('f_X(x) [V^{-1}]');
title('PDF: Gaussian Noise (σ = 0.5 V)');
grid on;

% Verify normalization (numerical integration)
dx = x(2) - x(1);
total_area = trapz(x, pdf_x);
fprintf('Total area under PDF = %.6f (should be 1)\n', total_area);

% Note: PDF values can exceed 1!
fprintf('Maximum PDF value = %.4f (can be > 1!)\n', max(pdf_x));

% Compute P(|X| > 1V)
prob_exceed = 1 - normcdf(1, 0, sigma) + normcdf(-1, 0, sigma);
fprintf('P(|X| > 1V) = %.4f\n', prob_exceed);
```

### مثال 3: مصورسازی CDF

```matlab
%% Comparing CDF for Discrete vs Continuous

figure;

% Subplot 1: Discrete CDF (Poisson, packets per second)
subplot(1,2,1);
lambda = 3;      % average arrivals per second
k = 0:10;
cdf_discrete = poisscdf(k, lambda);
stairs(k, cdf_discrete, 'r-', 'LineWidth', 2);
xlabel('Number of Packets');
ylabel('F_X(x) = P(X ≤ x)');
title('CDF: Discrete (Poisson, λ=3)');
grid on; ylim([0 1.1]);

% Subplot 2: Continuous CDF (Exponential, time to failure)
subplot(1,2,2);
mu = 5;          % mean time to failure (hours)
t = linspace(0, 20, 500);
cdf_continuous = expcdf(t, mu);
plot(t, cdf_continuous, 'b-', 'LineWidth', 2);
xlabel('Time (hours)');
ylabel('F_X(x) = P(X ≤ x)');
title('CDF: Continuous (Exponential, μ=5)');
grid on; ylim([0 1.1]);
```

### مثال 4: تبدیل یک متغیر تصادفی

```matlab
%% Transformation: Power from Noise Voltage
% X = noise voltage, Gaussian(0, sigma)
% Y = X^2 = instantaneous power (in 1-ohm load)

sigma = 1;
N = 100000;

% Generate Gaussian noise samples
X = sigma * randn(1, N);

% Transform: power = voltage^2
Y = X.^2;

% Plot empirical PDF of Y using histogram
figure;
subplot(2,1,1);
histogram(X, 100, 'Normalization', 'pdf');
hold on;
x_theory = linspace(-4, 4, 500);
plot(x_theory, normpdf(x_theory, 0, sigma), 'r-', 'LineWidth', 2);
xlabel('Voltage X'); ylabel('f_X(x)');
title('Input: Gaussian Noise Voltage');
legend('Empirical', 'Theoretical');

subplot(2,1,2);
histogram(Y, 100, 'Normalization', 'pdf');
hold on;
% Theoretical: Y ~ chi-squared with 1 degree of freedom
y_theory = linspace(0.01, 10, 500);
pdf_y_theory = chi2pdf(y_theory, 1);
plot(y_theory, pdf_y_theory, 'r-', 'LineWidth', 2);
xlabel('Power Y = X^2'); ylabel('f_Y(y)');
title('Output: Instantaneous Power');
legend('Empirical', 'Theoretical (χ²₁)');
```

### مثال 5: مقایسه PMF در برابر PDF در برابر CDF

```matlab
%% Complete Visualization: PMF/PDF and CDF side by side
figure;

% --- Discrete Case: Number of retransmissions ---
% Geometric distribution: number of trials until first success
p_success = 0.3;   % success probability per trial
k = 1:15;

% PMF
subplot(2,2,1);
pmf_geom = geopdf(k-1, p_success);  % MATLAB's geopdf counts failures
stem(k, pmf_geom, 'filled', 'LineWidth', 1.5);
xlabel('k (transmissions)'); ylabel('P(X = k)');
title('Discrete PMF: Retransmissions');
grid on;

% CDF
subplot(2,2,2);
cdf_geom = geocdf(k-1, p_success);
stairs(k, cdf_geom, 'LineWidth', 2);
xlabel('k'); ylabel('P(X ≤ k)');
title('Discrete CDF');
grid on; ylim([0 1.1]);

% --- Continuous Case: Delay time ---
% Exponential distribution
mu = 2;  % mean delay in ms
t = linspace(0, 10, 500);

% PDF
subplot(2,2,3);
pdf_exp = exppdf(t, mu);
plot(t, pdf_exp, 'LineWidth', 2);
xlabel('Delay (ms)'); ylabel('f_X(x) [ms^{-1}]');
title('Continuous PDF: Delay');
grid on;

% CDF
subplot(2,2,4);
cdf_exp = expcdf(t, mu);
plot(t, cdf_exp, 'LineWidth', 2);
xlabel('Delay (ms)'); ylabel('P(X ≤ x)');
title('Continuous CDF');
grid on; ylim([0 1.1]);

sgtitle('PMF/PDF vs CDF: Discrete and Continuous', 'FontSize', 14);
```

---

## 2.11 مسائل تمرینی

### مسئله 1: تشخیص نوع متغیر تصادفی
هر مورد را به‌عنوان گسسته یا پیوسته دسته‌بندی کنید و محدوده را بیان کنید:
(a) تعداد تماس‌های قطع‌شده در یک ساعت
(b) قدرت سیگنال دریافتی (dBm)
(c) تعداد بیت‌های دارای خطا در یک فریم اترنت 1500 بایتی
(d) زمان میان ورود بسته‌ها
(e) سطح خروجی کوانتایزر (کوانتایزر 3 بیتی)

**حل:**
(a) گسسته، $X \in \{0,\, 1,\, 2,\, \ldots \}$
(b) پیوسته، $X \in (-\infty,\, \infty)$ و در عمل حدود $\sim (-120,\, 0)$ dBm
(c) گسسته، $X \in \{0,\, 1,\, 2,\, \ldots,\, 12000\}$
(d) پیوسته، $T \in [0,\, \infty)$
(e) گسسته، $X \in \{0,\, 1,\, 2,\, 3,\, 4,\, 5,\, 6,\, 7\}$

---

### مسئله 2: ویژگی‌های PMF
یک متغیر تصادفی $X$ دارای PMF زیر است: $p_{X}(0) = 0.1$، $p_{X}(1) = 0.3$، $p_{X}(2) = c$، $p_{X}(3) = 0.2$.

(a) مقدار $c$ را بیابید.
(b) $P(X \ge 2)$ را بیابید.
(c) $F_{X}(1.5)$ را بیابید.
(d) $P(0.5 < X \le 2.5)$ را بیابید.

**حل:**
(a) مجموع = 1: $0.1 + 0.3 + c + 0.2 = 1 \to c = 0.4$

(b) $P(X \ge 2) = p_{X}(2) + p_{X}(3) = 0.4 + 0.2 = 0.6$

(c) $F_{X}(1.5) = P(X \le 1.5) = p_{X}(0) + p_{X}(1) = 0.1 + 0.3 = 0.4$

(d) $P(0.5 < X \le 2.5) = p_{X}(1) + p_{X}(2) = 0.3 + 0.4 = 0.7$

---

### مسئله 3: PDF و CDF
یک متغیر تصادفی دارای PDF زیر است:
$f_{X}(x)$ = cx برای $0 \le x \le 4$، و در غیر این صورت 0.

(a) مقدار $c$ را بیابید.
(b) تابع CDF یعنی $F_{X}(x)$ را بیابید.
(c) $P(1 \le X \le 3)$ را بیابید.
(d) مقدار $x_{0}$ را بیابید که در آن $P(X \le x_{0}) = 0.5$.

**حل:**
(a) $\int_{0}^{4}$ cx $dx = c \cdot \left[\frac{x^{2}}{2}\right]_{0}^{4} = c \cdot 8 = 1 \to c = \frac{1}{8}$

(b) $F_{X}(x) = \int_{0}^{x} \frac{t}{8}dt = \frac{x^{2}}{16}$، برای $0 \le x \le 4$
    $F_{X}(x) = 0$ برای $x < 0$؛ $F_{X}(x) = 1$ برای $x > 4$

(c) $P(1 \le X \le 3) = F_{X}(3) - F_{X}(1) = \frac{9}{16} - \frac{1}{16} = \frac{8}{16} = 0.5$

(d) $\frac{x_{0}^{2}}{16} = 0.5 \to x_{0}^{2} = 8 \to x_{0} = 2\sqrt{2} \approx 2.83$

---

### مسئله 4: تبدیل
یک حسگر دما $X$ را اندازه‌گیری می‌کند که به‌طور یکنواخت روی [20, 30] °C توزیع شده است.
ولتاژ خروجی $Y = 0.1X - 2$ است (حسگر خطی).

(a) PDF مربوط به $Y$ را بیابید.
(b) محدوده $Y$ را بیابید.
(c) $P(Y > 0.5)$ را بیابید.

**حل:**
(a) $X \sim \mathrm{Uniform}[20,\, 30]$، پس $f_{X}(x) = \frac{1}{10}$ برای $20 \le x \le 30$.
$Y = 0.1X - 2$، پس $a = 0.1$ و $b = -2$.
$f_{Y}(y) = \frac{1}{\lvert a\rvert} \cdot f_{X}\left(\frac{y-b}{a}\right) = \frac{1}{0.1} \cdot \frac{1}{10} = 1$ برای محدوده معتبر.

(b) وقتی $X = 20$: $Y = 0.1(20) - 2 = 0$؛ وقتی $X = 30$: $Y = 0.1(30) - 2 = 1$
محدوده $Y$: [0, 1]. پس $Y \sim \mathrm{Uniform}[0,\, 1]$.

(c) $P(Y > 0.5) = 1 - F_{Y}(0.5) = 1 - 0.5 = 0.5$

---

### مسئله 5: تبدیل غیریکنوا
یک ولتاژ $X \sim \mathrm{Uniform}[-1,\, 1] V$. توان لحظه‌ای $Y = X^{2}$ است.

(a) CDF مربوط به $Y$ را بیابید.
(b) PDF مربوط به $Y$ را بیابید.
(c) با شبیه‌سازی MATLAB بررسی کنید.

**حل:**
(a) برای $0 \le y \le 1$:
$F_{Y}(y) = P(X^{2} \le y) = P\left(-\sqrt{y} \le X \le \sqrt{y}\right) = F_{X}\left(\sqrt{y}\right) - F_{X}\left(-\sqrt{y}\right)$

از آنجا که $X \sim \mathrm{Uniform}[-1,\,1]$: $F_{X}(x) = \frac{x+1}{2}$

$F_{Y}(y) = \frac{\sqrt{y} + 1}{2} - \frac{-\sqrt{y} + 1}{2} = \sqrt{y}$

(b) $f_{Y}(y) = \frac{dF_{Y}}{dy} = \frac{1}{2\sqrt{y}}$ برای $0 < y \le 1$

(c) MATLAB:
```matlab
N = 100000;
X = 2*rand(1,N) - 1;   % Uniform[-1,1]
Y = X.^2;
histogram(Y, 100, 'Normalization', 'pdf');
hold on;
y = linspace(0.01, 1, 200);
plot(y, 1./(2*sqrt(y)), 'r-', 'LineWidth', 2);
legend('Empirical', 'Theory: 1/(2√y)');
xlabel('Y = X²'); ylabel('f_Y(y)');
```

---

## 2.12 نکات کلیدی

1. **متغیرهای تصادفی** خروجی‌های آزمایش را به اعداد برای تحلیل ریاضی تبدیل می‌کنند
2. **PMF** احتمال واقعی را برای متغیرهای تصادفی گسسته می‌دهد (ارتفاع میله‌ها جمعاً 1 می‌شود)
3. **PDF** چگالی احتمال را برای متغیرهای تصادفی پیوسته می‌دهد (مساحت احتمال را می‌دهد، نه ارتفاع)
4. **CDF** به‌طور کلی کار می‌کند — احتمال تجمعی $P(X \le x)$ را می‌دهد
5. **تبدیل‌ها** به ما امکان می‌دهند توزیع توابعی از متغیرهای تصادفی را استخراج کنیم
6. **روش CDF** (نخست CDF را بیابید، سپس مشتق بگیرید) اغلب ایمن‌ترین رویکرد برای تبدیل‌ها است

---

*ماژول بعدی: [ماژول 3 — توزیع‌های مهم احتمال](module03-important-distributions.md)*
<!-- TRANSLATION END -->
